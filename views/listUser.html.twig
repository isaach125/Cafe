{% extends 'template.html.twig' %}

{% block main %}

    Visitor list:
    <ul>
        {% for user in users %}

            <li>
                id({{ user.id }}) /
                {{ user.username }} /
                {{ user.password }}<br>
            </li>
        {% endfor %}
    </ul>

    <a href="index.php?action=updateListUser">Update a user</a><br>
    <a href="index.php?action=deleteListUser">Delete all users</a>
{% endblock %}